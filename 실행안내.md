# VHLookupLocal 실행 안내

비개발자 사용자는 아래 파일만 더블클릭하면 됩니다.

```text
dist\VHLookupLocal_pivot.exe
```

## 사용 순서

1. `dist\VHLookupLocal_pivot.exe`를 더블클릭합니다.
2. 실제 업무 파일은 아래 버튼 중 하나를 선택합니다.
   - `파일 문제 찾기(선택)`
   - `엑셀 파일 정리하기`
   - `분류별 시트 나누기`
   - `엑셀/CSV 파일 여러 개 합치기`
   - `전/후 파일 검증`
   - `피벗 요약표 만들기`
3. 파일을 올린 뒤 같은 창의 미리보기를 확인합니다.
4. 자동 추천이 틀리면 `컬럼 매칭 수정`을 눌러 드롭박스로 수정합니다.
5. `실행`을 누릅니다.
6. 결과는 자동으로 `outputs` 폴더에 저장되고 폴더가 열립니다.

## 4번 파일 합치기 사용법

1. `4. 엑셀/CSV 파일 여러 개 합치기`를 누릅니다.
2. `파일 올리기`로 합칠 파일을 선택합니다.
3. 아래 옵션 중 하나를 선택합니다.
   - `행 합치기`: 같은 양식의 여러 파일을 아래로 이어 붙입니다.
   - `열 합치기`: 공통 키를 찾아 다른 파일의 열을 오른쪽에 붙입니다.
4. 파일을 올리면 같은 창의 `미리보기` 영역에 예상 결과 10행이 바로 표시됩니다.
5. 자동 매칭이 틀리면 `컬럼 매칭 수정`을 눌러 드롭박스로 수정합니다.
6. `실행`을 누르면 바로 결과 파일을 만듭니다.

## 5번 전/후 파일 검증 사용법

1. `5. 전/후 파일 검증`을 누릅니다.
2. `수정 전` 파일과 `수정 후` 파일을 각각 선택합니다.
3. 두 파일을 모두 선택하면 같은 창의 `미리보기` 영역에 차이목록 10행이 바로 표시됩니다.
4. 자동 매칭이 틀리면 `컬럼 매칭 수정`을 눌러 비교 기준열과 전/후 컬럼 매칭을 수정합니다.
5. `실행`을 누르면 바로 결과 파일을 만듭니다.
6. 결과 파일의 `후파일_메모` 시트에서 노란색 셀의 메모를 확인합니다.
7. 전체 차이는 `차이목록`, 행 추가/누락은 `행비교`, 컬럼 추가/누락은 `컬럼비교`, 차이 있는 행만은 `차이행만`에서 확인합니다.

## 6번 피벗 요약표 만들기 사용법

1. `6. 피벗 요약표 만들기`를 누릅니다.
2. `파일 선택`으로 요약할 엑셀/CSV 파일을 선택합니다.
3. 드롭박스에서 `행 기준`, `열 기준`, `값 열`, `집계 방식`을 선택합니다.
4. `건수`는 값 열 없이 행 개수를 셉니다.
5. `합계`, `평균`, `최대`, `최소`는 숫자 값 열을 선택합니다.
6. 같은 창의 `미리보기`에서 요약표를 확인한 뒤 `실행`을 누릅니다.
7. 결과 파일의 `피벗요약`, `상위목록`, `기준설명`, `원본` 시트를 확인합니다.

## 중요한 점

- 원본 엑셀/CSV 파일은 수정하지 않습니다.
- 파일 합치기는 선택한 파일만 합칩니다. 폴더 안의 다른 파일을 마음대로 끼워 넣지 않습니다.
- 일반 엑셀/CSV 합치기는 원본 열을 삭제하지 않습니다.
- 키 컬럼과 가져올 컬럼은 기본적으로 자동 추천합니다.
- 실행 전 미리보기 화면에서 자동 추천된 컬럼 매칭을 사람이 수정할 수 있습니다.
- 일반 처리 결과에서 문제가 있는 행만 `오류행만` 시트로 따로 볼 수 있습니다.
- 확인할 내용은 결과 엑셀의 `먼저확인`, `확인필요`, `자동추천근거`, `개인정보점검` 시트에서 보면 됩니다.
- `개인정보점검` 시트에는 실제 개인정보 값이 아니라 유형과 건수만 표시됩니다.
- 샘플 데이터는 프로그램 버튼으로 따로 만들지 않고 `samples\public_admin` 폴더에 파일로 제공합니다.
- `파일 문제 찾기(선택)`은 필수 단계가 아닙니다. 파일 구조가 이상하거나 합치기 결과가 의심될 때만 사용하면 됩니다.
- 피벗 샘플은 `samples\public_admin\pivot_summary\budget_execution.csv`입니다.

## exe를 다시 만들 때

개발자 또는 배포 담당자만 아래 파일을 실행하면 됩니다.

```text
build_exe.bat
```
